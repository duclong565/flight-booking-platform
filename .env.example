# Copy to .env and override for non-local use. docker-compose reads .env automatically.
APP_JWT_SECRET=         # base64 HMAC secret (>=256 bits); leave blank to use the demo default
APP_ADMIN_USERNAME=admin
APP_ADMIN_PASSWORD=     # leave blank to use the demo default
