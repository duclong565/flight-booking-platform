/**
 * Shared, cross-service classes for the flight-booking platform
 * (standard error response shapes, common DTOs, etc.).
 *
 * <p>Intentionally minimal for now — concrete shared types are added once we
 * finalize the architecture.
 */
package com.deanflights.common;
